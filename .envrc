# Pin this project to pyenv 3.12.9 and activate local venv
export PYENV_VERSION=3.12.9

# Prevent stray conda envs from hijacking PATH
unset CONDA_PREFIX CONDA_DEFAULT_ENV CONDA_SHLVL

# Activate project venv
source ./.venv/bin/activate
